#include <stdio.h>

int main(){
    int contador;
    int numero = 2;

    for (contador = 2; contador<= 50; contador = contador + 2)
    {
        printf("Contador de numeros pares ate 50: %d \n", contador);
    }
    
}